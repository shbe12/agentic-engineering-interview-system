# CPU-only torch build — the default PyPI wheel bundles ~2.7GB of unused
# CUDA/cuDNN libraries (no GPU on serverless); this keeps the deploy small.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic-settings==2.7.1
anthropic==1.0.0
supabase==2.11.0
sentence-transformers==3.3.1
numpy==2.2.1
requests==2.32.3
python-dotenv==1.0.1
elevenlabs==2.64.0
pytest==8.3.4
httpx==0.28.1
