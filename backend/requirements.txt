fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic-settings==2.7.1
anthropic==1.0.0
supabase==2.11.0
# ML question retriever runs sentence-transformers/all-MiniLM-L6-v2 via ONNX
# Runtime (quantized weights, downloaded from HF Hub at runtime — see
# app/questions/retriever.py), not sentence-transformers/PyTorch. PyTorch
# alone is ~750MB even CPU-only — over Vercel's 500MB function size limit by
# itself, and both `sentence-transformers` and `optimum` list it as a hard
# dependency regardless of backend choice, so the only way to actually drop
# it is to bypass those libraries and call onnxruntime + a tokenizer
# directly. `transformers` here is for tokenization only (no torch extra).
onnxruntime==1.29.0
transformers==5.15.1
huggingface_hub==1.28.0
numpy==2.2.1
requests==2.32.3
python-dotenv==1.0.1
elevenlabs==2.64.0
pytest==8.3.4
httpx==0.28.1
