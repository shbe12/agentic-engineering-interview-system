# secrets — never commit
secrets.md
.env
.env.*
!.env.example

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# node
node_modules/
dist/
build/
.vite/

# data — ml_questions.md is committed (small, deterministic, needed in
# production so phase 4 doesn't silently fall back to Claude-generated-only)
*.faiss
*.index

# os / editor
.DS_Store
*.log
e2e/screenshots/
